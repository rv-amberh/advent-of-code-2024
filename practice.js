let input = [[1, 1, 2, 3], [1, 2, 3, 2], [2, 1, 2, 1], [2, 1, 1, 1]];

console.log(input, 'input');

//iterate through the nested array with [r, c] or [i, j] 

const findConnectedCells = (arrs) => {
    for (let i = 0; i < arrs.length - 1; i++) {
        for(let j = 0; j < arr[i].length; j++) {
            console.log(i, j)
        }
    }
 }